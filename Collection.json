{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "definitions": {"CollectionType": {
        "title":"Collection",
	"type": "object",
        "description": "The CollectionType captures collections of any MAEC entities, including Malware Subjects.",
        "properties": {
            "type": {
                "type": "string",
                "enum": ["collection"]
            },
            "id": {
                "type": "string",
                "description": "The required id field specifies a unique ID for this Collection."
            },
            "name": {
                "type": "string",
                "description": "The name field specifies the name of the Collection."
            },
            "maec_entity_type": {
                "$ref": "Vocabs.json#/definitions/collection-entity-cv",
                "description": "The required maec_entity_type field specifies the type of MAEC entity that is captured in the Collection, via the CollectionEntityTypeEnum. Example types would be 'objects' or 'various'. The default values is 'various'."
            },
            "association_type": {
                "$ref": "Vocabs.json#/definitions/collection-association-cv",
                "description": "The association type field specifies the nature of the contents of the Collection, via the CollectionAssociationTypeEnum."
            },
            "entity_refs": {
                "type": "array",
                "items": {"type": "string"},
                "description": "The entity_refs field captures a reference to one or more MAEC entities (Action, Behavior, etc.) that are contained in the Collection."
            },
            "association_metadata": {
                "$ref": "#/PLACEHOLDER FOR NOW - need to rethink this field",
                "description": "The association_metadata field captures metadata that may be relevant to specific association types as captured in the association_type field."
            }
        },
        "required": [
            "id",
            "maec_entity_type"
        ]
    }}
}
